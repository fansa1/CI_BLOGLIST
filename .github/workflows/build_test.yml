name: build_test

on:
  push:
    branches: [ master ]
  
  pull_request:
    branches: [master]
    types: [opened, synchronize]


jobs:

  test_bloglist_frontend_build:
    
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
     
        
      - name: npm install 
        run: npm install  
  
     # - name: build
     #   run: npm run build
        
     # - name: test frontend
     #  run: npm run test_frontend
    
    
      - name: create .env-file        
        run: |
          
          echo "$PATH"
          touch .env
          echo MONGODB_URI = ${{ secrets.MONGODB_URI }}
          echo PORT =  ${{ secrets.PORT }}
          echo TEST_MONGODB_URI =  ${{ secrets.TEST_MONGODB_URI }}
          echo SECRET = ${{ secrets.SECRET }}
          >> .env
          echo .env       
      #- name: test backend       
      #  run:  npm run test_backend
    
